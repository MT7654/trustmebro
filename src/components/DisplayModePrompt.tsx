import React,{useEffect,useState} from 'react';
import {Expand,Monitor} from 'lucide-react';
interface Props{onEnterFullscreen:()=>Promise<boolean>;onContinue:()=>void;isSupported:boolean;}
export const DisplayModePrompt:React.FC<Props>=({onEnterFullscreen,onContinue,isSupported})=>{const[error,setError]=useState(false);useEffect(()=>{const key=(e:KeyboardEvent)=>{if(e.key.toLowerCase()==='n'&&!(e.target instanceof HTMLInputElement))onContinue();};window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key);},[onContinue]);return <div className="grid h-dvh place-items-center overflow-hidden bg-slate-950 bg-cover bg-center p-5 text-white" style={{backgroundImage:"linear-gradient(rgba(2,6,23,.68),rgba(2,6,23,.9)),url('/art/environments/living-room-empty-master.png')"}}><section className="w-[min(92vw,680px)] rounded-3xl border border-cyan-300/35 bg-slate-950/88 p-7 text-center shadow-2xl backdrop-blur-md"><span className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-cyan-300 text-slate-950"><Monitor className="h-8 w-8"/></span><p className="mt-5 font-display text-xs font-black uppercase tracking-[.22em] text-cyan-300">Before you enter the room</p><h1 className="mt-2 font-heading text-3xl font-black">Best experienced full screen</h1><p className="mx-auto mt-4 max-w-xl leading-relaxed text-slate-300">Full screen removes browser distractions and makes the investigation easier to read. Windowed play is fully supported and uses the same controls.</p><div className="mt-7 grid gap-3 sm:grid-cols-2"><button disabled={!isSupported} onClick={async()=>{setError(false);if(await onEnterFullscreen())onContinue();else setError(true);}} className="flex min-h-14 items-center justify-center gap-2 rounded-xl bg-amber-300 px-5 font-heading font-black uppercase text-slate-950 disabled:opacity-40"><Expand/>Enter full screen</button><button onClick={onContinue} className="min-h-14 rounded-xl border border-slate-600 bg-slate-900 px-5 font-heading font-black uppercase">Continue windowed <span className="ml-2 text-xs text-slate-400">N</span></button></div>{error&&<p role="alert" className="mt-4 text-sm text-rose-300">The browser declined full screen. You can continue windowed and try again from the top bar.</p>}<p className="mt-5 text-xs text-slate-500">F11 may also toggle browser full screen. This game does not intercept that browser shortcut.</p></section></div>;};
